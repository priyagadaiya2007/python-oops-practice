[project]
name = "development"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Priya Gadaiya", email = "your-email@example.com" }
]
requires-python = ">=3.13"
dependencies = [
    "alembic>=1.19.1",
    "langchain>=1.3.16",
    "langchain-community>=0.4.2",
    "langgraph>=1.2.11",
    "mypy>=2.3.1",
    "pydantic>=2.13.4",
    "pydantic-settings>=2.15.0",
    "pytest>=9.1.1",
    "ruff>=0.16.4",
    "sqlalchemy>=2.0.52",
    "structlog>=26.1.0",
]

[project.scripts]
development = "development:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
